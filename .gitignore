# Data — сгенерированное (отчёты, сырые JSON скрейпа, history.csv) в git не идёт:
# облако после перезапуска показывало бы старый отчёт как свежий.
# Исключение — эталон клиента Parser.*: его коммитит persist_to_github
# (dedicated_app.py), из него регенерируется config/*.json.
data/*
!data/Parser.ods
!data/Parser.xlsx

# Claude Code
.claude/

# Internal planning
PRPs/
INITIAL.md

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
.env

# OS
.DS_Store
*.Zone.Identifier
